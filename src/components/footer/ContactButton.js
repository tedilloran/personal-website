import React from 'react';

class ContactButton extends React.Component {
  render() {
    return (
      <div />
      // <div style={{ textAlign: 'center' }}>
      //   <Button fluid color={'blue'} size={'huge'} href={this.props.URL}>
      //     <Icon name={this.props.icon} />
      //     {this.props.children}
      //   </Button>
      // </div>
    );
  }
}

export default ContactButton;
